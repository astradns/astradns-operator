apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "astradns.fullname" . }}-operator
  labels:
    {{- include "astradns.operator.labels" . | nindent 4 }}
rules:
  # CRD read/watch/update permissions
  - apiGroups:
      - dns.astradns.com
    resources:
      - dnsupstreampools
      - dnscacheprofiles
      - externaldnspolicies
    verbs:
      - get
      - list
      - watch
      - update
      - patch
  # CRD status subresource
  - apiGroups:
      - dns.astradns.com
    resources:
      - dnsupstreampools/status
      - dnscacheprofiles/status
      - externaldnspolicies/status
    verbs:
      - get
      - update
      - patch
  # ConfigMap management (agent config)
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      - create
      - update
  # Event recording
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
