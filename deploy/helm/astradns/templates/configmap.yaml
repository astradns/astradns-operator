apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "astradns.agent.configmap" . }}
  namespace: {{ include "astradns.namespace" . }}
  labels:
    {{- include "astradns.agent.labels" . | nindent 4 }}
data:
  config.json: "{}"
